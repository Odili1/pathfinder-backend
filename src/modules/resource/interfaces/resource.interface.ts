// import { Types } from "mongoose"

export interface IResource{
    avatar?: string,
    title?: string,
    category?: string,
    level?: string,
    noOfStudents?: string,
    courseOfDuration?: string,
    price?: string,
    levelOfExpertise?: string,
    noOfStudentGraduated?: string
    mentorId?: string
}